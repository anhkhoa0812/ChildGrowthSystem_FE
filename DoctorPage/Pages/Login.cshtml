@page
@model DoctorPage.Pages.LoginModel
@{
    ViewData["Title"] = "Login";
}

<style>
    body {
        background-size: cover;
        font-family: 'Helvetica Neue', Helvetica, Arial, sans-serif;
    }

    /* Centered login card styling */
    .login-container {
        background: rgba(255, 255, 255, 0.95);
        border-radius: 10px;
        padding: 30px;
        max-width: 400px;
        margin: 100px auto;
        box-shadow: 0 8px 16px rgba(0,0,0,0.2);
    }

    /* Header styling for the login card */
    .login-header {
        text-align: center;
        margin-bottom: 25px;
    }

    .login-header h1 {
        font-family: 'Georgia', serif;
        color: #2c3e50;
        font-size: 32px;
        margin-bottom: 10px;
    }

    /* Doctor themed button */
    .btn-doctor {
        background-color: #007bff;
        border-color: #007bff;
    }

    .btn-doctor:hover {
        background-color: #0056b3;
        border-color: #0056b3;
    }
</style>

<div class="login-container">
    <div class="login-header">
        <h1>Doctor Login</h1>
    </div>
    <form method="post">
        <div class="form-group">
            <label for="username">Email</label>
            <input type="text" asp-for="LoginRequest.Username" class="form-control" id="username" placeholder="Enter your email" />
            <span asp-validation-for="LoginRequest.Username" class="text-danger"></span>
        </div>
        <div class="form-group">
            <label for="password">Password</label>
            <input type="password" asp-for="LoginRequest.Password" class="form-control" id="password" placeholder="Password" />
            <span asp-validation-for="LoginRequest.Password" class="text-danger"></span>
        </div>
        <button type="submit" class="btn btn-doctor btn-block">Login</button>
    </form>

    @if (Model.Message != null)
    {
        <p class="text-danger mt-3">@Model.Message</p>
    }
</div>
